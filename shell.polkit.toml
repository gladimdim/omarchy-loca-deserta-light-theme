# Loca Deserta Light — black outline on the polkit
# authentication prompt. The wrong-password state stays red.

background       = "#ebe8e1"
background-alpha = 1.0
text             = "#1d2227"
text-error       = "#b8332c"
border           = "#000000"
border-error     = "#b8332c"
border-alpha     = 1.0
border-width     = 2
scrim            = "#ebe8e1"
scrim-alpha      = 0.5
accent           = "#1a6fa3"
